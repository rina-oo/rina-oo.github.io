# 논문 리뷰 작성 가이드

이 문서가 리뷰 형식의 **단일 기준**이다. 매일 실행되는 예약 에이전트와 수동 작성 모두 이 문서를 따른다.

목표는 "AI가 논문을 요약한 글"이 아니라 **사람이 논문을 읽고 소화해서 설명해주는 글**이다.

---

## 0. 작업 순서

```bash
# 1) 오늘 리뷰할 논문 선정
python3 scripts/select_paper.py
#    → _posts/ 에 오늘(KST) 날짜 포스트가 이미 있으면 아무것도 하지 말고 종료

# 2) 원문 확보 (LaTeX 원본 우선, 자동 폴백)
python3 scripts/fetch_paper.py <arxiv_id> --out /tmp/paper
#    → /tmp/paper/manifest.json 에 tier / figure / table 목록
#    → /tmp/paper/body.tex  본문
#    → /tmp/paper/figures/  렌더링된 그림
```

3. 원문을 읽고 리뷰 작성 → `_posts/YYYY-MM-DD-<arxiv_id>.md`
4. 그림을 `assets/img/posts/<arxiv_id>/`로 복사
5. 커밋 `post: <arxiv_id> <제목 요약> 리뷰` → push
6. 노션 "Daily Paper Reviews" DB에 1행 추가 (§6)

---

## 1. 원문은 어떻게 읽나 — 3단계 폴백

`fetch_paper.py`가 자동으로 가장 좋은 소스를 고른다. `manifest.json`의 `tier`를 보고 무엇을 얻었는지 파악한다.

| tier | 소스 | 수식 | 표 | 그림 |
|---|---|---|---|---|
| `latex-source` | arXiv `/e-print/` LaTeX 원본 | **원본 LaTeX 그대로** | `\textbf`/`\uline` 마커까지 보존 | 저자 원본 벡터 파일 |
| `arxiv-html` | arXiv `/html/` (LaTeXML) | MathML + TeX annotation | HTML 표 | `<img>` |
| `pdf-fallback` | PDF 텍스트 + 페이지 이미지 | ⚠ 깨짐 — 이미지로 확인 필수 | 눈으로 재구성 | 좌표 추정 캡처 |

**tier가 `pdf-fallback`이면** 수식은 텍스트만 믿지 말고 `/tmp/paper/pages/*.png` 페이지 이미지를 직접 보고 확인한다.

> ❌ 하지 말 것: abstract만 읽고 쓰기. 항상 본문을 읽는다.

---

## 2. 수식 처리 — "옮기지 말고 해석한다"

### 원칙
- **모든 수식을 넣지 않는다.** 논지에 필요한 것만 고른다. 보통 핵심 3~5개.
- 넣기로 한 수식은 반드시 **① 도입 문장 → ② 수식 → ③ 기호 설명 → ④ 의미 해석** 4단 구성.
- 수식을 넣었는데 그 아래에 아무 설명이 없으면 그 수식은 빼는 게 낫다.

### LaTeX 원본을 MathJax로 옮길 때
`body.tex`의 수식을 그대로 쓰되 다음만 치환한다:

| 원본 | 치환 | 이유 |
|---|---|---|
| `\bm{x}` | `\boldsymbol{x}` | MathJax 기본 미지원 |
| `\eqref{}`, `\label{}` | 삭제 | 블로그에 번호 참조 없음 |
| `\begin{equation}...\end{equation}` | `$$...$$` | Chirpy 기본 |

### 작성 예시

> KDA는 delta rule 재귀에 채널별 망각 게이트를 더한 구조입니다. 상태 $S_t$가 매 토큰마다 이렇게 갱신됩니다.
>
> $$S_t = \left(I - \beta_t k_t k_t^\top\right)\mathrm{Diag}(\alpha_t) S_{t-1} + \beta_t k_t v_t^\top$$
>
> $\alpha_t \in (0,1)^{d_k}$는 채널마다 따로 정해지는 보존 계수이고, $\beta_t$는 이번 토큰을 상태에 얼마나 세게 쓸지를 정하는 값입니다.
>
> 여기서 눈여겨볼 부분은 $\mathrm{Diag}(\alpha_t)$가 **채널별로** 붙는다는 점입니다. 기존 선형 어텐션은 상태 전체를 하나의 스칼라로 감쇠시켰는데, 채널마다 다른 망각 속도를 주면 "어떤 정보는 오래 붙들고 어떤 정보는 빨리 흘려보내는" 동작이 가능해집니다. 1M 토큰 컨텍스트에서 이 차이가 커집니다.

④번(의미 해석)이 이 글의 값어치다. ①~③만 있으면 논문 번역이다.

---

## 3. Figure 처리 — "붙이지 말고 읽어준다"

### 원칙
- `manifest.json`의 figure 목록에서 **논지에 필요한 것만** 고른다 (보통 3~5개). 전부 넣지 않는다.
- 넣을 그림은 **반드시 이미지를 직접 열어보고**(Read) 무엇이 그려져 있는지 확인한 뒤 쓴다. 캡션만 보고 쓰지 않는다.
- 그림마다 아래 3가지를 쓴다:
  1. **도입 문장** — "다음은 ~입니다" 식으로 무엇을 보여주는 그림인지
  2. **이미지 + 한국어 캡션** (원문 캡션 번역이 아니라 요약)
  3. **해석 문단** — 이 그림에서 **무엇을 읽어내야 하는지**. 축이 뭔지, 어느 지점을 봐야 하는지, 그래서 무슨 뜻인지.

### 이미지 배치

```bash
cp /tmp/paper/figures/fig7_scaling-law.png assets/img/posts/<arxiv_id>/
```

```markdown
다음은 K2와 K3의 스케일링 법칙을 비교한 그래프입니다.

![스케일링 법칙 비교](/assets/img/posts/<arxiv_id>/fig7_scaling-law.png)
_Figure 7. 같은 검증 손실에 도달하는 데 필요한 FLOPs 비교_

가로축이 학습에 쓴 연산량(FLOPs, 로그 스케일)이고 세로축이 검증 손실입니다.
같은 높이에서 두 선을 가로로 비교하면, K3(빨강)가 K2(파랑)보다 왼쪽에 있죠.
같은 성능을 2.5배 적은 연산으로 낸다는 뜻입니다. 두 선의 기울기가 거의 평행하다는
점도 중요한데, 개선이 특정 규모에서만 반짝하는 게 아니라 측정 구간 전체에서
유지된다는 의미이기 때문입니다.
```

### 최소 포함 대상
- 아키텍처/방법론 개요도 (있으면 반드시)
- 핵심 결과 그래프

---

## 4. Table 처리 — "원형 보존 + 해석"

- 표는 **이미지가 아니라 마크다운 표**로 옮긴다. 검색되고 모바일에서 읽힌다.
- **행을 임의로 추리거나 요약하지 않는다.** 논문 표를 그대로 재현한다.
- LaTeX 원본(`manifest.json`의 `tables[].latex`)에 서식 마커가 그대로 있으니 이를 따른다:
  - `\textbf{}` → **볼드** (1위)
  - `\uline{}` → _이탤릭_ (2위, 마크다운에 밑줄이 없으므로)
  - `\multicolumn`으로 묶인 그룹 헤더 → 표 안의 구분 행
- 표 아래에 **반드시 해석 문단**. 숫자를 나열하지 말고 **패턴**을 짚는다.
  - ❌ "GPQA에서 93.5점, HLE에서 56.0점을 기록했다"
  - ⭕ "직접 RL 환경을 만든 영역(에이전틱 검색·도구 사용)에서는 1위가 많은 반면, 연구급 추론(HLE·CritPt)에서는 일관되게 밀립니다. 훈련 환경으로 환원되는 능력과 그렇지 않은 능력이 갈리는 지점으로 보입니다."

---

## 5. 글의 형식과 문체

### Front matter
```yaml
---
title: "한국어 핵심 제목 — 원제 (Original Title)"
date: YYYY-MM-DD HH:MM:SS +0900
categories: [논문리뷰, Daily-Paper]
tags: [llm, moe, ...]     # 소문자 영문 3~6개
math: true
description: 한 문장 요약
---
```

> ⚠️ **`date`를 미래 시각으로 쓰면 안 된다.** Jekyll은 빌드 시점보다 미래인 포스트를 조용히 건너뛴다 — 빌드는 성공하는데 글만 안 올라가서 원인을 찾기 어렵다. `TZ=Asia/Seoul date`로 현재 시각을 확인하고 **그보다 과거**로 적는다.

### 글 구조

```markdown
> **원문**: [arXiv](...) · [HuggingFace](...) (👍 N) · [GitHub](...)
{: .prompt-info }

## 들어가며
(왜 이 논문인가 — 어제 HF에서 몇 표를 받았고, 무엇이 눈길을 끌었는지)

## 한눈에 보기
- **핵심 기여**: ...
- **성능**: ...
- **한 줄 요약**: ...

## 어떤 문제를 풀려고 했나
## 핵심 아이디어
## 실험 결과
## 아쉬운 점 / 의문점
## 마치며
(개인적 소감, 다음 관전 포인트)
```

### 문체 — **존댓말** (`~습니다`, `~입니다`)

한국 논문 리뷰 블로그를 8곳 살펴본 결과, 읽는 맛이 있는 글은 대부분 존댓말이었다. 평서체는 정확하지만 논문 번역문처럼 읽힌다.

| | 평서체 (지양) | 존댓말 (지향) |
|---|---|---|
| | "저자들은 ~을 제안하였다." | "저자들이 제안한 방법은 ~입니다." |
| | "성능이 2.5배 향상되었다." | "성능이 2.5배 좋아졌습니다." |

### 사람 냄새를 만드는 장치 (조사한 블로그들의 공통 습관)

1. **전문 용어는 영어 그대로 둔다** — 8곳 모두 그랬다. `fine-tuning`, `autoregressive`, `scalability`를 "미세조정", "자기회귀"로 억지 번역하지 않는다. 처음 나올 때만 짧게 풀어준다.
2. **논문 주장과 사실을 말투로 구분한다**
   - 논문이 주장하는 것: "~라고 합니다", "~라고 주장합니다"
   - 검증된 사실: "~입니다"
3. **왜 이런 선택을 했는지 맥락을 준다** — "그 당시 GPU가 3GB밖에 안 됐기 때문에 이런 구조가 나왔다고 합니다" 같은 배경. 논문 밖 정보(인용 수, 후속 연구, 커뮤니티 반응)를 곁들이면 좋다.
4. **비유를 쓴다** — 어려운 개념 하나 정도는 일상 비유로. "마치 ~처럼"
5. **모르는 건 모른다고 쓴다** — "이 부분은 논문에 명시되어 있지 않아서 정확히는 모르겠습니다", "왜 하필 이 값인지는 설명이 없네요"
6. **개인 의견을 명시적으로 표시한다** — "개인적으로는", "여담으로", "이건 좀 아쉬운데" 같은 마커로 논문 내용과 내 판단을 구분
7. **독자에게 말을 건다** — "한번 볼까요?", "여기서 눈여겨볼 부분은", "혹시 이 부분 아시는 분 계신가요?"
8. **비판적으로 읽는다** — 논문이 인정한 한계 + **리뷰어로서 던지는 의문**. ablation이 없다거나, 비교 조건이 공정한지 등.

### 절대 하지 말 것
- 논문에 없는 수치·주장 만들기. 불확실하면 "논문에 명시되지 않았습니다"라고 쓴다.
- 모든 섹션을 기계적으로 채우기. 할 말 없는 섹션은 빼는 게 낫다.
- 수식·그림·표를 설명 없이 나열하기.
- "본 논문은 ~을 제안한다"로 모든 문단 시작하기.

### 분량
2,000자 이상. 다만 분량을 채우려고 늘리지 않는다. 핵심 아이디어 섹션이 가장 길어야 한다.

---

## 6. 노션 기록

"Daily Paper Reviews" DB (data source `aea2861c-329d-43da-9003-2925f3b5e092`, Home 하위)에 1행 추가:

| 속성 | 내용 |
|---|---|
| `Title` | 논문 원제 |
| `Date` | 발행일 |
| `Source` | HF Daily Papers |
| `Upvotes`, `arXiv` | 선정 스크립트 출력 그대로 |
| `Category` | LLM/MoE/Vision/Multimodal/RL/Agent/Efficiency/Long-Context/Diffusion/Robotics 중 복수 선택 |
| `요약` | 2~3문장 |
| `흐름` | 논문 전개를 화살표로 한 줄 |
| `Contribution` | ①②③ 형식 |
| `Limitation` | 한계 요약 |
| `Blog Link` | `https://rina-oo.github.io/posts/<arxiv_id>/` |
| `도움 여부` | 아래 4개 중 하나 (필수) |
| `도움 이유` | 왜 그렇게 판단했는지 한 줄 |
| `GitHub` | **`🚫 스킵`이 아닐 때만** 코드 저장소 링크 |

### `도움 여부` 판단 기준

블로그 독자가 아니라 **이 블로그 주인(AI 전공 학부생, 연구·개인 프로젝트 진행 중)에게** 도움이 되는지를 기준으로 고른다.

| 옵션 | 언제 고르나 |
|---|---|
| 🔨 바로 써먹기 | 코드·도구가 공개돼 있고 지금 하는 작업에 바로 붙여볼 수 있다 |
| 💡 연구 아이디어 | 직접 돌리진 못해도 방법론·발상을 내 연구에 가져올 수 있다 |
| 📚 배경 지식 | 분야 흐름을 아는 데 필요하지만 당장 적용할 데는 없다 |
| 🚫 스킵 | 관심 분야와 거리가 멀거나 내용이 부실하다 |

- 규모가 커서 직접 못 돌리는 모델이라도, 작은 모델에 옮길 만한 설계가 있으면 `💡 연구 아이디어`다.
- 저장소 링크가 논문에 있어도 코드가 실제로 공개되지 않은 경우(placeholder)가 있으니, 링크를 넣기 전에 접근 가능한지 확인한다.
- 후하게 매기지 않는다. 전부 `🔨`면 이 필드는 쓸모가 없어진다.

---

## 7. 지식 레이어 (vault/) — 논문 사이 연결 축적

리뷰 발행 후, 이 저장소의 `vault/`에 지식 노트를 쌓는다. 이 디렉토리는 로컬 Research OS(Obsidian vault)로 동기화되어 검색·그래프·QA에 쓰이므로 **형식을 지키는 것이 중요**하다.

### 7-1. 논문 노트 (매일)

```bash
python3 scripts/build_knowledge.py <arxiv_id>
```

스크립트가 하는 일: `vault/papers/<arxiv_id>.md` 골격 생성(front matter + TL;DR 초안) → Semantic Scholar로 인용/피인용 조회 → **vault 안에 이미 있는 논문과의 교집합만** `cites`/`cited_by`와 `## 연결` 섹션에 양방향 기록 → `vault/topics/review-<tag>.md` 허브에 한 줄 추가. S2가 실패하면 pending에 기록되고 다음 실행 때 자동 재시도되니 신경 쓰지 않아도 된다.

**에이전트가 할 일**: 생성된 노트를 열어 세 섹션을 리뷰 내용으로 채운다.
- `## TL;DR` — 2~4문장. 무엇을 했고 왜 중요한지 (블로그 description보다 구체적으로)
- `## 핵심 기여` — 3~5개 불릿. 리뷰 본문의 핵심 아이디어 섹션 압축
- `## 한계` — 리뷰의 비판 섹션 압축 (논문 인정 한계 + 리뷰어 지적)

노트는 블로그 글이 아니라 **미래의 검색·연결을 위한 압축 지식**이다. 존댓말 불필요, 평서체로 밀도 있게. `## 연결` 섹션은 스크립트가 관리하므로 직접 수정하지 않는다.

### 7-2. 주간 종합 (일요일에만)

KST 기준 일요일이면, 이번 주(월~일) `vault/papers/` 노트들을 읽고 `vault/syntheses/YYYY-Www.md`(ISO 주차, 예: `2026-W31.md`)를 작성한다.

```yaml
---
title: '주간 종합: YYYY-Www'
type: synthesis
week: YYYY-Www
added: 'YYYY-MM-DD'
tags: [weekly-synthesis]
---
```

- 구성: **이번 주를 관통한 흐름 2~3개** → **논문 사이 연결점**(인용 관계, 같은 문제의 다른 접근, 상반된 주장) → **다음 주 주목할 것**
- 각 논문은 반드시 `[[<arxiv_id>|짧은 이름]]` 위키링크로 언급 (그래프에서 종합 노트가 허브가 됨)
- 목록 나열 금지 — "A와 B가 같은 병목을 다른 층에서 푼다" 같은 **관계 서술**이 목적
- 리뷰가 7편 미만이어도(주말 미발행 등) 있는 것만으로 작성
